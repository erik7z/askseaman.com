overwrite: true
schema: 'http://localhost:4000'
documents: null
generates:
  src/types/generated-backend.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      useIndexSignature: true
